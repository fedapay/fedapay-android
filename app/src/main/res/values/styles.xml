<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FedaUIStyle" parent="AppTheme" >
        <item name="colorPrimary">@color/fedapayUiPrimaryColor</item>
        <item name="colorPrimaryDark">@color/fedapayUiPrimaryColor</item>
        <item name="colorAccent">@color/fedapayUiSecondaryColor</item>
    </style>

</resources>
